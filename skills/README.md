# Skills

This directory will contain reusable, focused capabilities that agents can
discover and invoke.

Each skill should document its purpose, inputs, outputs, dependencies, and
compatibility constraints. Skills should remain independent of a particular AI
model whenever possible.
