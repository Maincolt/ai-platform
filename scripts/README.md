# Scripts

This directory is reserved for small, documented utilities that support local
development, validation, deployment, and maintenance.

Scripts should be repeatable, non-interactive where practical, and avoid
embedding environment-specific secrets or configuration.
