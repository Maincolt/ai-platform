# Local-only PostgreSQL and Apache Kafka (KRaft) topology for Sprint 6
# real-service validation. See infrastructure/README.md before use.
#
# This file is not a production deployment: single-node, self-signed-free
# SASL_PLAINTEXT, and locally generated credentials in infrastructure/compose/secrets/.

name: ai-platform-local

services:
  postgres:
    image: postgres:17.5-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_admin_password
      POSTGRES_DB: ai_platform
    secrets:
      - postgres_admin_password
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ai_platform"]
      interval: 5s
      timeout: 5s
      retries: 20

  postgres-init:
    image: postgres:17.5-alpine
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGHOST: postgres
      PGDATABASE: ai_platform
    secrets:
      - postgres_admin_password
      - postgres_orchestrator_migrator_password
      - postgres_orchestrator_app_password
      - postgres_agent_migrator_password
      - postgres_agent_app_password
    volumes:
      - ./scripts/init-postgres.sh:/init/init-postgres.sh:ro
      - ../postgresql:/sql/postgresql:ro
      - ../migrations:/sql/migrations:ro
    entrypoint: ["/bin/bash", "/init/init-postgres.sh"]
    restart: "no"

  kafka:
    image: apache/kafka:3.9.0
    hostname: kafka
    restart: unless-stopped
    environment:
      KAFKA_CLUSTER_ID: "${KAFKA_CLUSTER_ID:-9RIm8zoNSzqhz-gYo6F7MQ}"
      # Advertised to clients as the EXTERNAL listener's reconnect address
      # after the initial bootstrap -- must be reachable from wherever
      # clients actually run, not just from this host. Defaults to
      # "localhost" (correct only when the client runs on the same machine
      # as the broker); the current Docker host is reached remotely, so it
      # overrides this to its own LAN IP (see infrastructure/README.md).
      KAFKA_EXTERNAL_ADVERTISED_HOST: "${KAFKA_EXTERNAL_ADVERTISED_HOST:-localhost}"
    secrets:
      - kafka_admin_password
      - kafka_orchestrator_producer_password
      - kafka_orchestrator_consumer_password
      - kafka_agent_producer_password
      - kafka_agent_consumer_password
      - kafka_summarize_agent_producer_password
      - kafka_summarize_agent_consumer_password
      - kafka_review_agent_producer_password
      - kafka_review_agent_consumer_password
      - kafka_ui_review_agent_producer_password
      - kafka_ui_review_agent_consumer_password
    volumes:
      - ./kafka/entrypoint.sh:/opt/kafka-init/entrypoint.sh:ro
      - kafka-data:/var/lib/kafka/data
    entrypoint: ["/bin/bash", "/opt/kafka-init/entrypoint.sh"]
    ports:
      - "19093:19093"
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server localhost:9092 --command-config /tmp/admin-client.properties >/dev/null 2>&1"]
      interval: 10s
      timeout: 10s
      retries: 20
      start_period: 30s

  kafka-init:
    image: apache/kafka:3.9.0
    depends_on:
      kafka:
        condition: service_healthy
    secrets:
      - kafka_admin_password
    volumes:
      - ./scripts/init-kafka.sh:/init/init-kafka.sh:ro
    entrypoint: ["/bin/bash", "/init/init-kafka.sh"]
    restart: "no"

  # Application processes. Not started by a plain `up`; require
  # `docker compose --profile app up -d` after the image is built
  # (`docker build -f infrastructure/Dockerfile -t ai-platform:sprint6 .`
  # from the repository root) and after postgres-init/kafka-init have
  # completed successfully.
  platform:
    image: ai-platform:sprint6
    profiles: ["app"]
    restart: unless-stopped
    depends_on:
      postgres-init:
        condition: service_completed_successfully
      kafka-init:
        condition: service_completed_successfully
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_ORCHESTRATOR_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_ORCHESTRATOR_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_ORCHESTRATOR_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_ORCHESTRATOR_DATABASE_DSN_FILE: /run/secrets/dsn_orchestrator
      AI_PLATFORM_ORCHESTRATOR_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      # Fallback-only value (ADR-0014 Section 6): the Orchestrator's command
      # publisher now resolves the physical task-commands topic per
      # capability at publish time (KafkaEventPublisher._resolve_topic /
      # command_topic_binding_for_capability), so this is used only if a
      # command is ever published without a capability_name hint.
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.text-word-count.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.text-word-count.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_ORCHESTRATOR_KAFKA_PRODUCER_USERNAME: orchestrator-producer
      AI_PLATFORM_ORCHESTRATOR_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_orchestrator_producer_password
      AI_PLATFORM_ORCHESTRATOR_KAFKA_CONSUMER_USERNAME: orchestrator-consumer
      AI_PLATFORM_ORCHESTRATOR_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_orchestrator_consumer_password
      AI_PLATFORM_API_HOST: 127.0.0.1
      AI_PLATFORM_API_PORT: "8000"
      AI_PLATFORM_LOCAL_POLICY_ENABLED: "true"
      AI_PLATFORM_REGISTRY_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_ORCHESTRATOR_INSTANCE_ID: "01c0b1bd-a7f9-7765-acd3-b40d9e0b9dda"
      AI_PLATFORM_ORCHESTRATOR_OUTCOME_CONSUMER_GROUP_ID: ai-platform-orchestrator-outcomes
      AI_PLATFORM_ORCHESTRATOR_OUTCOME_LOGICAL_SUBSCRIPTION_ID: orchestrator-outcomes
      AI_PLATFORM_DEADLINE_INTERVAL_SECONDS: "5"
      AI_PLATFORM_DEADLINE_BATCH_SIZE: "50"
      AI_PLATFORM_AGENT_READINESS_TIMEOUT_SECONDS: "2"
      AI_PLATFORM_AGENT_READINESS_TTL_SECONDS: "10"
      AI_PLATFORM_AGENT_READINESS_REFRESH_INTERVAL_SECONDS: "5"
      AI_PLATFORM_TASK_RESULT_TIMEOUT_SECONDS: "60"
    secrets:
      - dsn_orchestrator
      - kafka_orchestrator_producer_password
      - kafka_orchestrator_consumer_password
      - readiness_credential
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro
    ports:
      - "8000:8000"
      - "8100:8100"
      - "8080:80"

  test-agent:
    image: ai-platform:sprint6
    profiles: ["app"]
    # Deliberately no restart policy: network_mode: "service:platform" below
    # ties this container to platform's specific network namespace. Docker
    # restarting *this* container would not fix a stale namespace reference
    # (only removing and recreating it does -- see docs/operations/README.md
    # Section 4), so an automatic restart here would just loop without ever
    # resolving DNS again. After any platform restart/reboot, recreate this
    # service explicitly: docker rm -f + `compose --profile app up -d test-agent`.
    command: ["ai-platform-test-agent"]
    network_mode: "service:platform"
    depends_on:
      platform:
        condition: service_started
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.text-word-count.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.text-word-count.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_USERNAME: agent-producer
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_agent_producer_password
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_USERNAME: agent-consumer
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_agent_consumer_password
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-123456789abc"
      AI_PLATFORM_AGENT_READINESS_HOST: 127.0.0.1
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_AGENT_MAXIMUM_CONCURRENCY: "4"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-test-agent
      AI_PLATFORM_AGENT_DECLARATION_DIGEST: "sha256:local-compose-declaration"
      AI_PLATFORM_AGENT_DECLARATION_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_AGENT_PUBLISHER_INSTANCE_ID: "5dfd3e0a-5e5e-72c0-95cc-c4cbb40fcfc9"
      AI_PLATFORM_AGENT_COMMAND_CONSUMER_GROUP_ID: ai-platform-agent-commands
      AI_PLATFORM_AGENT_COMMAND_LOGICAL_SUBSCRIPTION_ID: agent-commands
    secrets:
      - dsn_agent
      - kafka_agent_producer_password
      - kafka_agent_consumer_password
      - readiness_credential
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro

  # Second Test Agent replica, added ad hoc to validate Kafka assignment
  # fencing/rebalance for the ai-platform-agent-commands consumer group
  # (Sprint 6 remaining acceptance gap). Same consumer group and Kafka/DB
  # secrets as test-agent, but reaches kafka/postgres by service name on the
  # default Compose network rather than sharing platform's network
  # namespace, since it does not need the loopback-only readiness path.
  test-agent-2:
    image: ai-platform:sprint6
    profiles: ["app"]
    restart: unless-stopped
    command: ["ai-platform-test-agent"]
    depends_on:
      platform:
        condition: service_started
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.text-word-count.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.text-word-count.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_USERNAME: agent-producer
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_agent_producer_password
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_USERNAME: agent-consumer
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_agent_consumer_password
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-123456789abc"
      AI_PLATFORM_AGENT_READINESS_HOST: 127.0.0.1
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_AGENT_MAXIMUM_CONCURRENCY: "4"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-test-agent
      AI_PLATFORM_AGENT_DECLARATION_DIGEST: "sha256:local-compose-declaration"
      AI_PLATFORM_AGENT_DECLARATION_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_AGENT_PUBLISHER_INSTANCE_ID: "cc3f7cae-d72a-7bfd-8963-8f6b63260cd9"
      AI_PLATFORM_AGENT_COMMAND_CONSUMER_GROUP_ID: ai-platform-agent-commands
      AI_PLATFORM_AGENT_COMMAND_LOGICAL_SUBSCRIPTION_ID: agent-commands
    secrets:
      - dsn_agent
      - kafka_agent_producer_password
      - kafka_agent_consumer_password
      - readiness_credential
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro

  # The second built-in Agent class (ADR-0014): text.summarize, backed by
  # the AI Router. Reuses the same generic ai-platform-test-agent console
  # script as test-agent/test-agent-2 -- build_agent_process() selects the
  # SummarizeAgent executor from this deployment's registry.json binding
  # (capability_name == "text.summarize"), not from the process name. Not
  # network_mode: "service:platform" (that trick is test-agent-specific);
  # reaches kafka/postgres by service name like test-agent-2, with its own
  # loopback-only readiness listener on its own container network namespace.
  summarize-agent:
    image: ai-platform:sprint6
    profiles: ["app"]
    restart: unless-stopped
    command: ["ai-platform-test-agent"]
    depends_on:
      platform:
        condition: service_started
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.text-summarize.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.text-summarize.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_USERNAME: summarize-agent-producer
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_summarize_agent_producer_password
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_USERNAME: summarize-agent-consumer
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_summarize_agent_consumer_password
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-abcdef987654"
      # 0.0.0.0, not loopback (ADR-0017 Decision 5): unlike test-agent,
      # summarize-agent does not share platform's network namespace, so a
      # loopback bind would only ever be reachable from inside this
      # container -- never from platform, which needs to query it over
      # the Compose network at summarize-agent:8100 (see registry.json).
      AI_PLATFORM_AGENT_READINESS_HOST: 0.0.0.0
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_AGENT_MAXIMUM_CONCURRENCY: "4"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-summarize-agent
      AI_PLATFORM_AGENT_DECLARATION_DIGEST: "sha256:local-compose-summarize-declaration"
      AI_PLATFORM_AGENT_DECLARATION_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_AGENT_PUBLISHER_INSTANCE_ID: "1f2e3d4c-5b6a-7c91-8a2e-9f8e7d6c5b4a"
      AI_PLATFORM_AGENT_COMMAND_CONSUMER_GROUP_ID: ai-platform-summarize-agent-commands
      AI_PLATFORM_AGENT_COMMAND_LOGICAL_SUBSCRIPTION_ID: summarize-agent-commands
      # AI Router (ADR-0014). The referenced keys are checked-in, obviously-
      # fake Sprint 9 placeholders (infrastructure/README.md) -- no real
      # Anthropic/OpenAI credentials exist in this environment, so real
      # text.summarize submissions fail at the provider call, not at startup.
      # Models must be on the ADR-0017 Decision 3 approved list
      # (runtime/composition.py's _APPROVED_ANTHROPIC_MODELS/
      # _APPROVED_OPENAI_MODELS) -- summarize-agent fails closed at
      # startup on any other value.
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_API_KEY_FILE: /run/secrets/ai_router_anthropic_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_MODEL: claude-haiku-4-5
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_API_KEY_FILE: /run/secrets/ai_router_openai_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_MODEL: gpt-5-mini
      AI_PLATFORM_AGENT_AI_ROUTER_MAX_OUTPUT_TOKENS: "512"
      AI_PLATFORM_AGENT_AI_ROUTER_PROVIDER_TIMEOUT_SECONDS: "20"
    secrets:
      - dsn_agent
      - kafka_summarize_agent_producer_password
      - kafka_summarize_agent_consumer_password
      - readiness_credential
      - ai_router_anthropic_api_key
      - ai_router_openai_api_key
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro

  review-agent:
    image: ai-platform:sprint6
    profiles: ["app"]
    restart: unless-stopped
    command: ["ai-platform-test-agent"]
    depends_on:
      platform:
        condition: service_started
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.code-review.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.code-review.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_USERNAME: review-agent-producer
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_review_agent_producer_password
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_USERNAME: review-agent-consumer
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_review_agent_consumer_password
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-fedcba123456"
      # 0.0.0.0, not loopback (ADR-0017 Decision 5): review-agent does not
      # share platform's network namespace, so a loopback bind would only
      # ever be reachable from inside this container -- never from
      # platform, which needs to query it over the Compose network at
      # review-agent:8100 (see registry.json).
      AI_PLATFORM_AGENT_READINESS_HOST: 0.0.0.0
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_AGENT_MAXIMUM_CONCURRENCY: "4"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-review-agent
      AI_PLATFORM_AGENT_DECLARATION_DIGEST: "sha256:local-compose-review-declaration"
      AI_PLATFORM_AGENT_DECLARATION_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_AGENT_PUBLISHER_INSTANCE_ID: "2a3b4c5d-6e7f-7a91-8a2e-1a2b3c4d5e6f"
      AI_PLATFORM_AGENT_COMMAND_CONSUMER_GROUP_ID: ai-platform-review-agent-commands
      AI_PLATFORM_AGENT_COMMAND_LOGICAL_SUBSCRIPTION_ID: review-agent-commands
      # AI Router (ADR-0014). ADR-0018 Decision 5 deliberately left
      # code.review's own model approval undecided at acceptance time; the
      # repository owner has since decided (during this deployment-wiring
      # PR) to reuse ADR-0017 Decision 3's exact approved list and the same
      # checked-in, obviously-fake placeholder credentials summarize-agent
      # already uses -- no real Anthropic/OpenAI credentials exist in this
      # environment, so a real code.review submission would fail at the
      # provider call, not at startup. Models must be on the ADR-0017
      # Decision 3 approved list (runtime/composition.py's
      # _APPROVED_ANTHROPIC_MODELS/_APPROVED_OPENAI_MODELS) -- review-agent
      # fails closed at startup on any other value.
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_API_KEY_FILE: /run/secrets/ai_router_anthropic_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_MODEL: claude-haiku-4-5
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_API_KEY_FILE: /run/secrets/ai_router_openai_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_MODEL: gpt-5-mini
      AI_PLATFORM_AGENT_AI_ROUTER_MAX_OUTPUT_TOKENS: "512"
      AI_PLATFORM_AGENT_AI_ROUTER_PROVIDER_TIMEOUT_SECONDS: "20"
    secrets:
      - dsn_agent
      - kafka_review_agent_producer_password
      - kafka_review_agent_consumer_password
      - readiness_credential
      - ai_router_anthropic_api_key
      - ai_router_openai_api_key
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro

  # Shares platform's network namespace, the same way test-agent does
  # (see that service's comment below): nginx proxies to platform's
  # Workflow API at 127.0.0.1:8000, which is intentionally loopback-only
  # (docs/operations/README.md Section 8) -- this reaches it without
  # widening that exposure to the whole Compose network. Port 8080 is
  # therefore published on platform itself, not here (network_mode:
  # "service:platform" containers cannot declare their own ports:).
  dashboard:
    build:
      context: ../../frontend/dashboard
      dockerfile: Dockerfile
    profiles: ["app"]
    # No restart policy, same reasoning as test-agent above: this shares
    # platform's network namespace via network_mode, so it needs recreating
    # (not restarting) whenever platform restarts.
    network_mode: "service:platform"
    depends_on:
      platform:
        condition: service_started

  # ADR-0019: ui.review, the platform's third AI-backed capability. Built
  # from its own dedicated Dockerfile, not the shared ai-platform:sprint6
  # image every other service here uses -- this is the only service that
  # needs Chromium, and a dedicated image keeps that cost (several hundred
  # MB, real build time) isolated to just this one service (ADR-0019
  # Decision 5). The allowed review target is hardcoded in
  # runtime/composition.py's _UI_REVIEW_ALLOWED_TARGET, not an environment
  # variable here -- ADR-0019 Decision 4 deliberately leaves no
  # configuration path that could widen it.
  ui-review-agent:
    build:
      context: ../..
      dockerfile: infrastructure/ui-review-agent/Dockerfile
    profiles: ["app"]
    restart: unless-stopped
    command: ["ai-platform-test-agent"]
    depends_on:
      platform:
        condition: service_started
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.ui-review.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.ui-review.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_USERNAME: ui-review-agent-producer
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_ui_review_agent_producer_password
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_USERNAME: ui-review-agent-consumer
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_ui_review_agent_consumer_password
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-c0ffee123456"
      # 0.0.0.0, not loopback (ADR-0017 Decision 5): ui-review-agent does
      # not share platform's network namespace, so a loopback bind would
      # only ever be reachable from inside this container -- never from
      # platform, which needs to query it over the Compose network at
      # ui-review-agent:8100 (see registry.json).
      AI_PLATFORM_AGENT_READINESS_HOST: 0.0.0.0
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_AGENT_MAXIMUM_CONCURRENCY: "4"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-ui-review-agent
      AI_PLATFORM_AGENT_DECLARATION_DIGEST: "sha256:local-compose-ui-review-declaration"
      AI_PLATFORM_AGENT_DECLARATION_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_AGENT_PUBLISHER_INSTANCE_ID: "4c5d6e7f-8091-7ca2-9b3e-3c4d5e6f7081"
      AI_PLATFORM_AGENT_COMMAND_CONSUMER_GROUP_ID: ai-platform-ui-review-agent-commands
      AI_PLATFORM_AGENT_COMMAND_LOGICAL_SUBSCRIPTION_ID: ui-review-agent-commands
      # AI Router (ADR-0014). ADR-0019 Decision 3 reuses ADR-0017 Decision
      # 3's exact approved model list and the same checked-in, obviously-
      # fake placeholder credentials summarize-agent/review-agent already
      # use -- no real Anthropic/OpenAI credentials exist in this
      # environment, so a real ui.review submission would fail at the
      # provider call, not at startup. Models must be on the ADR-0017
      # Decision 3 approved list (runtime/composition.py's
      # _APPROVED_ANTHROPIC_MODELS/_APPROVED_OPENAI_MODELS) -- ui-review-agent
      # fails closed at startup on any other value.
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_API_KEY_FILE: /run/secrets/ai_router_anthropic_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_MODEL: claude-haiku-4-5
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_API_KEY_FILE: /run/secrets/ai_router_openai_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_MODEL: gpt-5-mini
      AI_PLATFORM_AGENT_AI_ROUTER_MAX_OUTPUT_TOKENS: "512"
      AI_PLATFORM_AGENT_AI_ROUTER_PROVIDER_TIMEOUT_SECONDS: "20"
    secrets:
      - dsn_agent
      - kafka_ui_review_agent_producer_password
      - kafka_ui_review_agent_consumer_password
      - readiness_credential
      - ai_router_anthropic_api_key
      - ai_router_openai_api_key
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro

  # ADR-0020: architecture.review, the platform's fifth capability. Built
  # from the shared ai-platform:sprint6 image, same as review-agent --
  # unlike ui-review-agent, this needs no new runtime dependency.
  architecture-review-agent:
    image: ai-platform:sprint6
    profiles: ["app"]
    restart: unless-stopped
    command: ["ai-platform-test-agent"]
    depends_on:
      platform:
        condition: service_started
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.architecture-review.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.architecture-review.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_USERNAME: architecture-review-agent-producer
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_architecture_review_agent_producer_password
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_USERNAME: architecture-review-agent-consumer
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_architecture_review_agent_consumer_password
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-a12ec7ec7000"
      # 0.0.0.0, not loopback (ADR-0017 Decision 5): architecture-review-agent
      # does not share platform's network namespace, so a loopback bind
      # would only ever be reachable from inside this container -- never
      # from platform, which needs to query it over the Compose network at
      # architecture-review-agent:8100 (see registry.json).
      AI_PLATFORM_AGENT_READINESS_HOST: 0.0.0.0
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_AGENT_MAXIMUM_CONCURRENCY: "4"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-architecture-review-agent
      AI_PLATFORM_AGENT_DECLARATION_DIGEST: "sha256:local-compose-architecture-review-declaration"
      AI_PLATFORM_AGENT_DECLARATION_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_AGENT_PUBLISHER_INSTANCE_ID: "5d6e7f80-91a2-7db3-8c4f-4d5e6f708192"
      AI_PLATFORM_AGENT_COMMAND_CONSUMER_GROUP_ID: ai-platform-architecture-review-agent-commands
      AI_PLATFORM_AGENT_COMMAND_LOGICAL_SUBSCRIPTION_ID: architecture-review-agent-commands
      # AI Router (ADR-0014). ADR-0020 Decision 3 reuses ADR-0017 Decision
      # 3's exact approved model list unchanged. Real Anthropic (primary)
      # and OpenAI (fallback) credentials are configured in this
      # environment (see ADR-0018/ADR-0019's Implementation Status) --
      # architecture-review-agent uses the same real credentials. Models
      # must still be on the ADR-0017 Decision 3 approved list
      # (runtime/composition.py's _APPROVED_ANTHROPIC_MODELS/
      # _APPROVED_OPENAI_MODELS) -- architecture-review-agent fails closed
      # at startup on any other value.
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_API_KEY_FILE: /run/secrets/ai_router_anthropic_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_MODEL: claude-haiku-4-5
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_API_KEY_FILE: /run/secrets/ai_router_openai_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_MODEL: gpt-5-mini
      AI_PLATFORM_AGENT_AI_ROUTER_MAX_OUTPUT_TOKENS: "512"
      AI_PLATFORM_AGENT_AI_ROUTER_PROVIDER_TIMEOUT_SECONDS: "20"
    secrets:
      - dsn_agent
      - kafka_architecture_review_agent_producer_password
      - kafka_architecture_review_agent_consumer_password
      - readiness_credential
      - ai_router_anthropic_api_key
      - ai_router_openai_api_key
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro

  # ADR-0021: data.analysis, the platform's sixth capability. Built from
  # the shared ai-platform:sprint6 image, same as architecture-review-agent.
  data-analysis-agent:
    image: ai-platform:sprint6
    profiles: ["app"]
    restart: unless-stopped
    command: ["ai-platform-test-agent"]
    depends_on:
      platform:
        condition: service_started
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.data-analysis.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.data-analysis.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_USERNAME: data-analysis-agent-producer
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_data_analysis_agent_producer_password
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_USERNAME: data-analysis-agent-consumer
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_data_analysis_agent_consumer_password
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-da7a04a15000"
      # 0.0.0.0, not loopback (ADR-0017 Decision 5): data-analysis-agent
      # does not share platform's network namespace, so a loopback bind
      # would only ever be reachable from inside this container -- never
      # from platform, which needs to query it over the Compose network at
      # data-analysis-agent:8100 (see registry.json).
      AI_PLATFORM_AGENT_READINESS_HOST: 0.0.0.0
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_AGENT_MAXIMUM_CONCURRENCY: "4"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-data-analysis-agent
      AI_PLATFORM_AGENT_DECLARATION_DIGEST: "sha256:local-compose-data-analysis-declaration"
      AI_PLATFORM_AGENT_DECLARATION_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_AGENT_PUBLISHER_INSTANCE_ID: "6e7f8091-a2b3-7ec4-9d5e-5f6070819293"
      AI_PLATFORM_AGENT_COMMAND_CONSUMER_GROUP_ID: ai-platform-data-analysis-agent-commands
      AI_PLATFORM_AGENT_COMMAND_LOGICAL_SUBSCRIPTION_ID: data-analysis-agent-commands
      # AI Router (ADR-0014). ADR-0021 Decision 3 reuses ADR-0017 Decision
      # 3's exact approved model list unchanged. Real Anthropic (primary)
      # and OpenAI (fallback) credentials are configured in this
      # environment -- data-analysis-agent uses the same real credentials.
      # Models must still be on the ADR-0017 Decision 3 approved list
      # (runtime/composition.py's _APPROVED_ANTHROPIC_MODELS/
      # _APPROVED_OPENAI_MODELS) -- data-analysis-agent fails closed at
      # startup on any other value.
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_API_KEY_FILE: /run/secrets/ai_router_anthropic_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_MODEL: claude-haiku-4-5
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_API_KEY_FILE: /run/secrets/ai_router_openai_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_MODEL: gpt-5-mini
      AI_PLATFORM_AGENT_AI_ROUTER_MAX_OUTPUT_TOKENS: "512"
      AI_PLATFORM_AGENT_AI_ROUTER_PROVIDER_TIMEOUT_SECONDS: "20"
    secrets:
      - dsn_agent
      - kafka_data_analysis_agent_producer_password
      - kafka_data_analysis_agent_consumer_password
      - readiness_credential
      - ai_router_anthropic_api_key
      - ai_router_openai_api_key
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro

  # ADR-0022: technical.review, the platform's seventh capability. Built
  # from the shared ai-platform:sprint6 image, same as data-analysis-agent.
  technical-review-agent:
    image: ai-platform:sprint6
    profiles: ["app"]
    restart: unless-stopped
    command: ["ai-platform-test-agent"]
    depends_on:
      platform:
        condition: service_started
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.technical-review.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.technical-review.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_USERNAME: technical-review-agent-producer
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_technical_review_agent_producer_password
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_USERNAME: technical-review-agent-consumer
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_technical_review_agent_consumer_password
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-7ec4b1ca1000"
      # 0.0.0.0, not loopback (ADR-0017 Decision 5): technical-review-agent
      # does not share platform's network namespace, so a loopback bind
      # would only ever be reachable from inside this container -- never
      # from platform, which needs to query it over the Compose network at
      # technical-review-agent:8100 (see registry.json).
      AI_PLATFORM_AGENT_READINESS_HOST: 0.0.0.0
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_AGENT_MAXIMUM_CONCURRENCY: "4"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-technical-review-agent
      AI_PLATFORM_AGENT_DECLARATION_DIGEST: "sha256:local-compose-technical-review-declaration"
      AI_PLATFORM_AGENT_DECLARATION_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_AGENT_PUBLISHER_INSTANCE_ID: "7f8091a2-b3c4-7ed5-8e6f-6f7081929394"
      AI_PLATFORM_AGENT_COMMAND_CONSUMER_GROUP_ID: ai-platform-technical-review-agent-commands
      AI_PLATFORM_AGENT_COMMAND_LOGICAL_SUBSCRIPTION_ID: technical-review-agent-commands
      # AI Router (ADR-0014). ADR-0022 Decision 3 reuses ADR-0017 Decision
      # 3's exact approved model list unchanged. Real Anthropic (primary)
      # and OpenAI (fallback) credentials are configured in this
      # environment -- technical-review-agent uses the same real
      # credentials. Models must still be on the ADR-0017 Decision 3
      # approved list (runtime/composition.py's
      # _APPROVED_ANTHROPIC_MODELS/_APPROVED_OPENAI_MODELS) --
      # technical-review-agent fails closed at startup on any other value.
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_API_KEY_FILE: /run/secrets/ai_router_anthropic_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_MODEL: claude-haiku-4-5
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_API_KEY_FILE: /run/secrets/ai_router_openai_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_MODEL: gpt-5-mini
      AI_PLATFORM_AGENT_AI_ROUTER_MAX_OUTPUT_TOKENS: "512"
      AI_PLATFORM_AGENT_AI_ROUTER_PROVIDER_TIMEOUT_SECONDS: "20"
    secrets:
      - dsn_agent
      - kafka_technical_review_agent_producer_password
      - kafka_technical_review_agent_consumer_password
      - readiness_credential
      - ai_router_anthropic_api_key
      - ai_router_openai_api_key
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro

  # ADR-0025: security.review, the platform's ninth capability. Built from
  # the shared ai-platform:sprint6 image, same as technical-review-agent.
  security-review-agent:
    image: ai-platform:sprint6
    profiles: ["app"]
    restart: unless-stopped
    command: ["ai-platform-test-agent"]
    depends_on:
      platform:
        condition: service_started
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.security-review.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.security-review.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_USERNAME: security-review-agent-producer
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_security_review_agent_producer_password
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_USERNAME: security-review-agent-consumer
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_security_review_agent_consumer_password
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-5ec0517924e5"
      # 0.0.0.0, not loopback (ADR-0017 Decision 5): security-review-agent
      # does not share platform's network namespace, so a loopback bind
      # would only ever be reachable from inside this container -- never
      # from platform, which needs to query it over the Compose network at
      # security-review-agent:8100 (see registry.json).
      AI_PLATFORM_AGENT_READINESS_HOST: 0.0.0.0
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_AGENT_MAXIMUM_CONCURRENCY: "4"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-security-review-agent
      AI_PLATFORM_AGENT_DECLARATION_DIGEST: "sha256:local-compose-security-review-declaration"
      AI_PLATFORM_AGENT_DECLARATION_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_AGENT_PUBLISHER_INSTANCE_ID: "91a2b3c4-d5e6-7f70-8081-929394a5b6c7"
      AI_PLATFORM_AGENT_COMMAND_CONSUMER_GROUP_ID: ai-platform-security-review-agent-commands
      AI_PLATFORM_AGENT_COMMAND_LOGICAL_SUBSCRIPTION_ID: security-review-agent-commands
      # AI Router (ADR-0014). ADR-0025 Decision 4 reuses ADR-0017 Decision
      # 3's exact approved model list unchanged. Real Anthropic (primary)
      # and OpenAI (fallback) credentials are configured in this
      # environment -- security-review-agent uses the same real
      # credentials. Models must still be on the ADR-0017 Decision 3
      # approved list (runtime/composition.py's
      # _APPROVED_ANTHROPIC_MODELS/_APPROVED_OPENAI_MODELS) --
      # security-review-agent fails closed at startup on any other value.
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_API_KEY_FILE: /run/secrets/ai_router_anthropic_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_MODEL: claude-haiku-4-5
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_API_KEY_FILE: /run/secrets/ai_router_openai_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_MODEL: gpt-5-mini
      AI_PLATFORM_AGENT_AI_ROUTER_MAX_OUTPUT_TOKENS: "512"
      AI_PLATFORM_AGENT_AI_ROUTER_PROVIDER_TIMEOUT_SECONDS: "20"
    secrets:
      - dsn_agent
      - kafka_security_review_agent_producer_password
      - kafka_security_review_agent_consumer_password
      - readiness_credential
      - ai_router_anthropic_api_key
      - ai_router_openai_api_key
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro

  # ADR-0027: scrum.status, the platform's tenth capability -- ADR-0026's
  # Phase 1. Built from the shared ai-platform:sprint6 image, same as
  # security-review-agent. AI_PLATFORM_AGENT_GITHUB_TOKEN_FILE points at a
  # real read:project-scoped PAT (ADR-0027 Decision 5); the project
  # coordinates below point at the repository owner's real Projects v2
  # board.
  scrum-status-agent:
    image: ai-platform:sprint6
    profiles: ["app"]
    restart: unless-stopped
    command: ["ai-platform-test-agent"]
    depends_on:
      platform:
        condition: service_started
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.scrum-status.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.scrum-status.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_USERNAME: scrum-status-agent-producer
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_scrum_status_agent_producer_password
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_USERNAME: scrum-status-agent-consumer
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_scrum_status_agent_consumer_password
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-5c40b57a7a75"
      # 0.0.0.0, not loopback (ADR-0017 Decision 5): scrum-status-agent
      # does not share platform's network namespace, so a loopback bind
      # would only ever be reachable from inside this container -- never
      # from platform, which needs to query it over the Compose network at
      # scrum-status-agent:8100 (see registry.json).
      AI_PLATFORM_AGENT_READINESS_HOST: 0.0.0.0
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_AGENT_MAXIMUM_CONCURRENCY: "4"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-scrum-status-agent
      AI_PLATFORM_AGENT_DECLARATION_DIGEST: "sha256:local-compose-scrum-status-declaration"
      AI_PLATFORM_AGENT_DECLARATION_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_AGENT_PUBLISHER_INSTANCE_ID: "a2b3c4d5-e6f7-7081-8192-a3b4c5d6e7f8"
      AI_PLATFORM_AGENT_COMMAND_CONSUMER_GROUP_ID: ai-platform-scrum-status-agent-commands
      AI_PLATFORM_AGENT_COMMAND_LOGICAL_SUBSCRIPTION_ID: scrum-status-agent-commands
      # AI Router (ADR-0014). ADR-0027 Decision 6 reuses ADR-0017 Decision
      # 3's exact approved model list unchanged. Real Anthropic (primary)
      # and OpenAI (fallback) credentials are configured in this
      # environment -- scrum-status-agent uses the same real credentials.
      # Models must still be on the ADR-0017 Decision 3 approved list
      # (runtime/composition.py's
      # _APPROVED_ANTHROPIC_MODELS/_APPROVED_OPENAI_MODELS) --
      # scrum-status-agent fails closed at startup on any other value.
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_API_KEY_FILE: /run/secrets/ai_router_anthropic_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_MODEL: claude-haiku-4-5
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_API_KEY_FILE: /run/secrets/ai_router_openai_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_MODEL: gpt-5-mini
      AI_PLATFORM_AGENT_AI_ROUTER_MAX_OUTPUT_TOKENS: "512"
      AI_PLATFORM_AGENT_AI_ROUTER_PROVIDER_TIMEOUT_SECONDS: "20"
      # ADR-0027 Decision 4: config-driven, not hardcoded (unlike
      # ui.review's allowed target) -- no SSRF-shaped risk here, since the
      # fetch always goes to api.github.com, scoped by whichever
      # read:project-only PAT is configured.
      AI_PLATFORM_AGENT_GITHUB_TOKEN_FILE: /run/secrets/github_token
      AI_PLATFORM_AGENT_GITHUB_PROJECT_OWNER: "Maincolt"
      AI_PLATFORM_AGENT_GITHUB_PROJECT_NUMBER: "1"
    secrets:
      - dsn_agent
      - kafka_scrum_status_agent_producer_password
      - kafka_scrum_status_agent_consumer_password
      - readiness_credential
      - ai_router_anthropic_api_key
      - ai_router_openai_api_key
      - github_token
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro

  # ADR-0026/ADR-0028: scrum-master-agent, ADR-0026 Phase 2 -- the first
  # Agent deployable with zero Kafka wiring and no Capability Registry
  # binding. It consumes no ExecuteTask commands; it wakes up hourly
  # (PeriodicService) and, subject to the kill switch and daily
  # action/spend cap, takes real autonomous write actions against the
  # same GitHub Projects v2 board scrum-status-agent reads. Its PAT
  # (AI_PLATFORM_AGENT_GITHUB_TOKEN_FILE) is deliberately a *different*,
  # separately-scoped credential from scrum-status-agent's (ADR-0028
  # Decision 4, per-role least privilege) -- never share the two.
  scrum-master-agent:
    image: ai-platform:sprint6
    profiles: ["app"]
    restart: unless-stopped
    command: ["ai-platform-scrum-master-agent"]
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "3"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-5c40a5750001"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-scrum-master-agent
      # 0.0.0.0, not loopback (ADR-0017 Decision 5): scrum-master-agent
      # does not share platform's network namespace. Readiness here is a
      # Docker-healthcheck-only signal (ADR-0028 Decision 6) -- this
      # agent is not a Capability Registry binding and platform never
      # queries it.
      AI_PLATFORM_AGENT_READINESS_HOST: 0.0.0.0
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      # AI Router (ADR-0014), reusing ADR-0017 Decision 3's exact
      # approved model list unchanged -- same as every other capability.
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_API_KEY_FILE: /run/secrets/ai_router_anthropic_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_MODEL: claude-haiku-4-5
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_API_KEY_FILE: /run/secrets/ai_router_openai_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_MODEL: gpt-5-mini
      AI_PLATFORM_AGENT_AI_ROUTER_MAX_OUTPUT_TOKENS: "512"
      AI_PLATFORM_AGENT_AI_ROUTER_PROVIDER_TIMEOUT_SECONDS: "20"
      # ADR-0028 Decision 4: a separate, scrum-master-only PAT (project +
      # repo scopes) -- never scrum-status-agent's read-only token.
      AI_PLATFORM_AGENT_GITHUB_TOKEN_FILE: /run/secrets/github_token_scrum_master
      AI_PLATFORM_AGENT_GITHUB_PROJECT_OWNER: "Maincolt"
      AI_PLATFORM_AGENT_GITHUB_PROJECT_NUMBER: "1"
      # ADR-0028 Decision 2: repository owner's explicit choices.
      AI_PLATFORM_AGENT_AUTONOMOUS_POLL_INTERVAL_SECONDS: "3600"
      AI_PLATFORM_AGENT_AUTONOMOUS_MAX_ACTIONS_PER_DAY: "10"
      AI_PLATFORM_AGENT_AUTONOMOUS_MAX_SPEND_CENTS_PER_DAY: "100"
    secrets:
      - dsn_agent
      - readiness_credential
      - ai_router_anthropic_api_key
      - ai_router_openai_api_key
      - github_token_scrum_master

  # ADR-0023: assignment.route, the platform's eighth capability. Built
  # from the shared ai-platform:sprint6 image, same as technical-review-agent.
  assignment-route-agent:
    image: ai-platform:sprint6
    profiles: ["app"]
    restart: unless-stopped
    command: ["ai-platform-test-agent"]
    depends_on:
      platform:
        condition: service_started
    environment:
      AI_PLATFORM_ENVIRONMENT: development
      AI_PLATFORM_AGENT_KAFKA_SECURITY_PROTOCOL: LOCAL_DEVELOPMENT_SASL_PLAINTEXT
      AI_PLATFORM_AGENT_DATABASE_POOL_MIN_SIZE: "1"
      AI_PLATFORM_AGENT_DATABASE_POOL_MAX_SIZE: "5"
      AI_PLATFORM_AGENT_DATABASE_DSN_FILE: /run/secrets/dsn_agent
      AI_PLATFORM_AGENT_DATABASE_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      AI_PLATFORM_TASK_COMMANDS_TOPIC: ai-platform.development.task-commands.assignment-route.v1
      AI_PLATFORM_TASK_COMMANDS_QUARANTINE_TOPIC: ai-platform.development.task-commands.assignment-route.v1.quarantine
      AI_PLATFORM_TASK_OUTCOMES_TOPIC: ai-platform.development.task-outcomes.v1
      AI_PLATFORM_TASK_OUTCOMES_QUARANTINE_TOPIC: ai-platform.development.task-outcomes.v1.quarantine
      AI_PLATFORM_CONTRACT_SCHEMA_DIRECTORY: /app/contracts/json-schema/v1
      AI_PLATFORM_PUBLISH_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_CONSUMER_POLL_TIMEOUT_SECONDS: "1"
      AI_PLATFORM_CONSUMER_RETRY_DELAY_SECONDS: "2"
      AI_PLATFORM_CONSUMER_MAXIMUM_PROCESSING_ATTEMPTS: "5"
      AI_PLATFORM_WORKER_IDLE_DELAY_SECONDS: "0.5"
      AI_PLATFORM_OUTBOX_CLAIM_TTL_SECONDS: "30"
      AI_PLATFORM_OUTBOX_MAXIMUM_PUBLICATION_ATTEMPTS: "5"
      AI_PLATFORM_QUARANTINE_RECONCILIATION_LIMIT: "50"
      AI_PLATFORM_QUARANTINE_QUERY_TIMEOUT_SECONDS: "5"
      AI_PLATFORM_STARTUP_TIMEOUT_SECONDS: "30"
      AI_PLATFORM_SHUTDOWN_GRACE_SECONDS: "10"
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_USERNAME: assignment-route-agent-producer
      AI_PLATFORM_AGENT_KAFKA_PRODUCER_PASSWORD_FILE: /run/secrets/kafka_assignment_route_agent_producer_password
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_USERNAME: assignment-route-agent-consumer
      AI_PLATFORM_AGENT_KAFKA_CONSUMER_PASSWORD_FILE: /run/secrets/kafka_assignment_route_agent_consumer_password
      AI_PLATFORM_AGENT_ID: "018f23a7-6b4d-7c91-8a2e-a551900e7000"
      # 0.0.0.0, not loopback (ADR-0017 Decision 5): assignment-route-agent
      # does not share platform's network namespace, so a loopback bind
      # would only ever be reachable from inside this container -- never
      # from platform, which needs to query it over the Compose network at
      # assignment-route-agent:8100 (see registry.json).
      AI_PLATFORM_AGENT_READINESS_HOST: 0.0.0.0
      AI_PLATFORM_AGENT_READINESS_PORT: "8100"
      AI_PLATFORM_READINESS_CREDENTIAL_FILE: /run/secrets/readiness_credential
      AI_PLATFORM_AGENT_MAXIMUM_CONCURRENCY: "4"
      AI_PLATFORM_AGENT_COMPONENT: local-compose-assignment-route-agent
      AI_PLATFORM_AGENT_DECLARATION_DIGEST: "sha256:local-compose-assignment-route-declaration"
      AI_PLATFORM_AGENT_DECLARATION_PATH: /app/runtime-config/registry.json
      AI_PLATFORM_AGENT_PUBLISHER_INSTANCE_ID: "8091a2b3-c4d5-7ee6-9f70-7081929394a5"
      AI_PLATFORM_AGENT_COMMAND_CONSUMER_GROUP_ID: ai-platform-assignment-route-agent-commands
      AI_PLATFORM_AGENT_COMMAND_LOGICAL_SUBSCRIPTION_ID: assignment-route-agent-commands
      # AI Router (ADR-0014). ADR-0023 Decision 3 reuses ADR-0017 Decision
      # 3's exact approved model list unchanged. Real Anthropic (primary)
      # and OpenAI (fallback) credentials are configured in this
      # environment -- assignment-route-agent uses the same real
      # credentials. Models must still be on the ADR-0017 Decision 3
      # approved list (runtime/composition.py's
      # _APPROVED_ANTHROPIC_MODELS/_APPROVED_OPENAI_MODELS) --
      # assignment-route-agent fails closed at startup on any other value.
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_API_KEY_FILE: /run/secrets/ai_router_anthropic_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_ANTHROPIC_MODEL: claude-haiku-4-5
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_API_KEY_FILE: /run/secrets/ai_router_openai_api_key
      AI_PLATFORM_AGENT_AI_ROUTER_OPENAI_MODEL: gpt-5-mini
      AI_PLATFORM_AGENT_AI_ROUTER_MAX_OUTPUT_TOKENS: "512"
      AI_PLATFORM_AGENT_AI_ROUTER_PROVIDER_TIMEOUT_SECONDS: "20"
    secrets:
      - dsn_agent
      - kafka_assignment_route_agent_producer_password
      - kafka_assignment_route_agent_consumer_password
      - readiness_credential
      - ai_router_anthropic_api_key
      - ai_router_openai_api_key
    volumes:
      - ./runtime/registry.json:/app/runtime-config/registry.json:ro

secrets:
  postgres_admin_password:
    file: ./secrets/postgres_admin_password.txt
  postgres_orchestrator_migrator_password:
    file: ./secrets/postgres_orchestrator_migrator_password.txt
  postgres_orchestrator_app_password:
    file: ./secrets/postgres_orchestrator_app_password.txt
  postgres_agent_migrator_password:
    file: ./secrets/postgres_agent_migrator_password.txt
  postgres_agent_app_password:
    file: ./secrets/postgres_agent_app_password.txt
  kafka_admin_password:
    file: ./secrets/kafka_admin_password.txt
  kafka_orchestrator_producer_password:
    file: ./secrets/kafka_orchestrator_producer_password.txt
  kafka_orchestrator_consumer_password:
    file: ./secrets/kafka_orchestrator_consumer_password.txt
  kafka_agent_producer_password:
    file: ./secrets/kafka_agent_producer_password.txt
  kafka_agent_consumer_password:
    file: ./secrets/kafka_agent_consumer_password.txt
  kafka_summarize_agent_producer_password:
    file: ./secrets/kafka_summarize_agent_producer_password.txt
  kafka_summarize_agent_consumer_password:
    file: ./secrets/kafka_summarize_agent_consumer_password.txt
  kafka_review_agent_producer_password:
    file: ./secrets/kafka_review_agent_producer_password.txt
  kafka_review_agent_consumer_password:
    file: ./secrets/kafka_review_agent_consumer_password.txt
  kafka_ui_review_agent_producer_password:
    file: ./secrets/kafka_ui_review_agent_producer_password.txt
  kafka_ui_review_agent_consumer_password:
    file: ./secrets/kafka_ui_review_agent_consumer_password.txt
  kafka_architecture_review_agent_producer_password:
    file: ./secrets/kafka_architecture_review_agent_producer_password.txt
  kafka_architecture_review_agent_consumer_password:
    file: ./secrets/kafka_architecture_review_agent_consumer_password.txt
  kafka_data_analysis_agent_producer_password:
    file: ./secrets/kafka_data_analysis_agent_producer_password.txt
  kafka_data_analysis_agent_consumer_password:
    file: ./secrets/kafka_data_analysis_agent_consumer_password.txt
  kafka_technical_review_agent_producer_password:
    file: ./secrets/kafka_technical_review_agent_producer_password.txt
  kafka_technical_review_agent_consumer_password:
    file: ./secrets/kafka_technical_review_agent_consumer_password.txt
  kafka_security_review_agent_producer_password:
    file: ./secrets/kafka_security_review_agent_producer_password.txt
  kafka_security_review_agent_consumer_password:
    file: ./secrets/kafka_security_review_agent_consumer_password.txt
  kafka_scrum_status_agent_producer_password:
    file: ./secrets/kafka_scrum_status_agent_producer_password.txt
  kafka_scrum_status_agent_consumer_password:
    file: ./secrets/kafka_scrum_status_agent_consumer_password.txt
  kafka_assignment_route_agent_producer_password:
    file: ./secrets/kafka_assignment_route_agent_producer_password.txt
  kafka_assignment_route_agent_consumer_password:
    file: ./secrets/kafka_assignment_route_agent_consumer_password.txt
  dsn_orchestrator:
    file: ./secrets/dsn_orchestrator.txt
  dsn_agent:
    file: ./secrets/dsn_agent.txt
  readiness_credential:
    file: ./secrets/readiness_credential.txt
  ai_router_anthropic_api_key:
    file: ./secrets/ai_router_anthropic_api_key.txt
  ai_router_openai_api_key:
    file: ./secrets/ai_router_openai_api_key.txt
  # ADR-0027 Decision 5: a real GitHub PAT scoped to read:project only.
  # Starts as an obviously-fake placeholder (same pattern the AI Router
  # provider keys followed) until a real Projects v2 board exists.
  github_token:
    file: ./secrets/github_token.txt
  # ADR-0028 Decision 4: a separate, scrum-master-only PAT (project +
  # repo scopes) -- deliberately never the same credential as
  # github_token above (per-role least privilege). Placeholder until the
  # repository owner supplies a real one.
  github_token_scrum_master:
    file: ./secrets/github_token_scrum_master.txt

volumes:
  postgres-data:
  kafka-data:
