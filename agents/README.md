# Agents

This directory will contain independently deployable agent definitions and their
supporting metadata.

Each agent should have one clearly documented responsibility, communicate
through platform events, and avoid direct dependencies on other agents.

Agent-specific implementation and configuration will be added after the
relevant architecture decisions are approved.
