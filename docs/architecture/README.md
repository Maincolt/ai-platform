# Architecture

This directory will describe the platform's system boundaries, components,
interfaces, event flows, deployment topology, and cross-cutting concerns.

Architecture documentation should remain vendor-neutral unless a documented
decision deliberately selects a technology for a bounded purpose.

Record significant decisions in `decisions/`.
